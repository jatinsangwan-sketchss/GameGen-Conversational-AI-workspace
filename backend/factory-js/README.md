# factory-js (WIP)

Node.js / JavaScript port of the AI Game Factory.

This directory is created as a safe migration layer so we can:
- keep the Python backend intact while porting
- validate parity through shared JSON artifacts

