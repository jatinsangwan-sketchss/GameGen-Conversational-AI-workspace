The previous JSON output is invalid.

Your task is to correct it so it matches the required schema and remains grounded in the original source documents.

Rules:
- Output valid JSON only.
- Do not include markdown.
- Do not include explanations.
- Do not invent unsupported systems or gameplay features.
- Correct only what is necessary.

Schema errors:
{schema_errors}

Previous JSON:
{previous_json}

Original source context:
PRD:
{prd_text}

GDD:
{gdd_text}

UI spec:
{ui_spec_text}